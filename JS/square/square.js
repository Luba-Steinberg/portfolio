function squarearea() {
let height;
let width;
let area;
let hekef;
height = prompt ("What is the height?");
width = prompt ("What is the width?");
area= height*width;
alert ("The square of the square is " +area);
hekef = height*2 + width*2
alert ("The scope of the square is " +hekef)
}